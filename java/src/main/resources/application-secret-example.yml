spring:
  data:
    mongodb:
      uri: mongodb+srv://도메인.mongodb.net/?retryWrites=true&w=majority
      database: 디비
      username: 유저
      password: 비번

  mail:
    host: smtp.gmail.com
    port: 587
    username: a@gmail.com
    password: '1234 abcd'
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

jwt:
  issuer: 이슈
  secret_key: 키
  # openssl rand -hex 32

logging:
  level:
    org:
      springframework:
        security: DEBUG

unprotected:
  routes: /login,/user/signup,/user/forgot-password,/user/reset-password