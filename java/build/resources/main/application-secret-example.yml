spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  data:
    mongodb:
      uri: mongodb+srv://도메인.mongodb.net/?retryWrites=true&w=majority
      database: 디비
      username: 유저
      password: 비번

jwt:
  issuer: 이슈
  secret_key: 키
  # openssl rand -hex 32

logging:
  level:
    org:
      springframework:
        security: DEBUG